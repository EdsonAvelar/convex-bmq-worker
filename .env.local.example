# ============================================================================
# DESENVOLVIMENTO LOCAL COM DOCKER COMPOSE
# ============================================================================
# Copie este arquivo para .env para rodar localmente com docker-compose
# O docker-compose.yml já sobrescreve REDIS_URL para usar o Redis do container

# ⚠️ IMPORTANTE: NÃO defina UPSTASH_REDIS_URL aqui!
# Se você definir, o código vai ignorar o Redis local e usar Upstash

# Redis - DEIXE COMENTADO para desenvolvimento local
# O docker-compose.yml define automaticamente: REDIS_URL=redis://redis:6379
# REDIS_URL=redis://localhost:6379  # Use apenas se rodar FORA do docker

# Secret compartilhado - OBRIGATÓRIO
QUEUE_WORKER_SECRET="408c02491b2cb008aaf853a46144844abf3ef6c08ddf621c3072314fbffb8a02"

# ============================================================================
# VARIÁVEIS QUE NÃO DEVEM ESTAR AQUI PARA DEV LOCAL
# ============================================================================
# ❌ NÃO defina estas variáveis para desenvolvimento local:
# UPSTASH_REDIS_URL - Vai usar o Upstash ao invés do Redis local
# UPSTASH_REDIS_REST_URL - Desnecessário para dev local
# UPSTASH_REDIS_REST_TOKEN - Desnecessário para dev local
