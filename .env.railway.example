# ============================================================================
# RAILWAY PRODUCTION ENVIRONMENT
# ============================================================================
# Configure estas variáveis no Railway Dashboard → Variables

# ============================================================================
# OPÇÃO 1: Redis do Railway (RECOMENDADO)
# ============================================================================
# Adicione um Redis Database no Railway
# A variável REDIS_URL será automaticamente criada
# Formato: redis://default:password@redis.railway.internal:6379

# ============================================================================
# OPÇÃO 2: Redis Upstash (se preferir usar serviço externo)
# ============================================================================
# Use se quiser Redis gerenciado externamente
# REDIS_URL=rediss://default:SEU_PASSWORD@seu-redis.upstash.io:6379

# ============================================================================
# OBRIGATÓRIO
# ============================================================================
# Secret compartilhado com Next.js (mesmo valor em ambos)
QUEUE_WORKER_SECRET=your_super_secret_token_here_min_32_chars

# ============================================================================
# OPCIONAL - Configurações de Worker
# ============================================================================
# Quantos jobs processar simultaneamente (padrão: 10)
# WORKER_CONCURRENCY=10

# Tempo máximo para processar um job em ms (padrão: 60000)
# WORKER_LOCK_DURATION=60000

# Porta do servidor (Railway usa PORT automaticamente)
# PORT=3002

# ============================================================================
# NOTAS IMPORTANTES
# ============================================================================
# ⚠️ NÃO use UPSTASH_REDIS_URL - use apenas REDIS_URL
# ✅ Local: docker-compose sobrescreve para redis://redis:6379
# ✅ Railway: Configure REDIS_URL com Redis do Railway ou Upstash
# 🔧 O código prioriza REDIS_URL para evitar conflitos
